postgres = {
  url = "jdbc:postgresql://localhost:5432/test"
  driver = org.postgresql.Driver
  connectionPool = disabled
  keepAliveConnection = true
}

slick-code-generator {
  init-scripts = [./db/src/test/sql/genesis.sql]
  excluded-tables = []
}
